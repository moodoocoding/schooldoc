# 1인 1역 검증 보고서

## 확인한 흐름

교사 설정의 명단 → 역할 2단계 배정 → 학급 공용 학생 화면 → 오늘 실천 저장/재제출 → 교사 정정 → 선택한 월의 학생별 누가 기록.

## 로컬 검사 결과 (2026-09-27)

| 검사 | 결과 | 범위 |
| --- | --- | --- |
| `npm run typecheck` | 통과 | 프런트엔드 + 공유 도메인 |
| `npm run lint` | 통과 | 신규 코드 경고 없음. 기존 코드 경고 6건 유지 |
| `npm test` | 48파일 / 392검사 통과 | 현재 작업 폴더의 기존 미커밋 영수증 검사 포함 |
| 관련 Playwright E2E | 22검사 통과 | 1인 1역 7개, 셸 스크롤 15개 |
| `npm run build` | 통과 | 기존 큰 청크 경고 있음 |
| Deno 함수 타입 검사 | 통과 | 신규 admin/public 서버 함수 |
| 서버 HTTP 계약 검사 | 10검사 통과 | 가상 fetch, 실제 네트워크 사용 안 함 |
| PostgreSQL SQL 검사 | 1검사 / 8단계 통과 | PGlite 메모리 DB, 신규 마이그레이션·RLS·RPC |
| 브라우저 시각 검사 | 통과 | 홈의 6개 기능, 데스크톱/모바일 캡처 확인 |

서버 HTTP 검사는 인증 필수·소유자 필터·공개 토큰·공개 응답 최소화·날짜/상태 위조 거부·공유 중지·요청 제한 실패·동시 변경 충돌·월간 1,050행 조회·암호화 키 누락을 검사했다.

SQL 검사는 실제 PostgreSQL 문법으로 마이그레이션 적용, 교사별 읽기 격리, 브라우저 직접 쓰기/RPC 거부, 익명 접근 거부, 중복 제출 갱신, 버전 충돌, 다른 교사 기록 차단, 한 건 초기화를 검사했다. 운영 Supabase 검증으로 간주하지 않는다.

## 수정하고 재검증한 문제

1. 공유 입력 검증의 TypeScript narrowing 오류 → 타입이 확정된 지역 변수 및 명시적 종료 함수 사용.
2. 월간 기록이 기본 1,000행 제한에 잘릴 가능성 → 500행 페이지 조회, 1,050행 회귀 테스트.
3. 공개 객체 펼치기로 향후 추가 필드가 노출될 가능성 → 공개 필드 명시적 선택.
4. 입력 라벨을 정확히 찾지 못한 브라우저 검사 실패 → 고유 id/htmlFor와 명시적 선택상자 이름, 22검사 재통과.
5. 기존 QR 테스트의 Windows 경로 구분자 불일치 → 플랫폼별 `join()` 경로, 기존 기능 변경 없음.

## 독립 체크아웃 재검증

커밋 `03be934`만 체크아웃한 독립 검증에서 `typecheck`, 린트, 단위 테스트 46파일/378검사, 빌드, 서버 11검사(내부 SQL 8단계)를 다시 통과했다. 원래 작업 폴더와의 14개 검사 차이는 커밋하지 않은 사용자 영수증 테스트이며 삭제하지 않았다.

독립 설치에서 기존 Vitest 4.1.10의 개발 도구 보안 경고가 발견되어 공식 패치 4.1.11을 적용했다. 패치 후 결과는 아래 추가 검증에 기록한다.

### 보안 패치 추가 검증

- Vitest 관련 개발 패키지 8개만 4.1.11로 갱신했고 운영 의존성은 유지했다.
- 기본 npm 10.9.2의 peer dependency 해석 오류는 시스템 npm을 교체하지 않고 임시 npm 11로 잠금 파일을 생성해 해결했다. 이후 독립 체크아웃에서 일반 `npm ci`가 통과함을 확인했다.
- 원래 작업 폴더에서 패치 후 타입 검사·린트·단위 검사 392개·프로덕션 빌드를 재통과했다. 의존성 감사 취약점은 0건이다.
- 패치 커밋 `f5f2517`의 독립 체크아웃에서도 일반 npm 설치, 단위 378개, 타입, 빌드, Chrome E2E 22개를 재통과했다.

### GitHub 상태

- 브랜치: `codex/classroom-roles`.
- [PR #16](https://github.com/moodoocoding/schooldoc/pull/16): main 통합 완료 (`d515eee`).
- `f5f2517`의 Vercel 브랜치 미리보기 빌드 성공을 GitHub 배포 검사에서 확인했다. 서버 기능의 운영 검증을 의미하지 않는다.
- 통합 커밋의 Vercel Production 배포 성공을 확인했고 운영 주소에서 아래 인수 검사를 통과했다.

- GitHub 로그인과 기존 운영 Git 연동 배포 이력은 확인했다.
- Supabase CLI 로그인 후 운영 앱의 공개 연결 호스트와 프로젝트를 대조했다.
- 연결된 Vercel 계정은 이 저장소의 기존 팀에 접근 권한이 없다. 프런트엔드 배포는 기존 GitHub 연동 경로를 사용하되 결과를 확인해야 한다.
- 실제 물리 기기 간 시험과 Google OAuth 상호작용은 하지 않았다. 서로 저장 공간을 공유하지 않는 독립 브라우저 컨텍스트의 실제 서버 동기화를 검사했다.
- 외부 개발 일지와 `pro/ux-ui-expert.md`는 이 checkout에 없어 사용하지 않았다. 계획·모의 검토·검증 내역은 이 폴더에 기록했다.

### 운영 서버 배포·실제 연동 검사 (2026-09-27)

- 기존 DB 이력이 모두 일치하고 새 마이그레이션 한 개만 미적용임을 `migration list --linked`와 `db push --dry-run`으로 확인했다.
- 기존 키를 보존하고 `CLASSROOM_ROLES_ENCRYPTION_KEY`만 신규 등록했다. 값은 Git·로그에 남기지 않았다. 로컬 복구본은 Git 제외 폴더의 Windows 사용자 암호화(DPAPI) 형태로 보관했다.
- `202609270001_classroom_roles.sql` 적용 후 원격 이력 일치를 재확인했다.
- `classroom-roles-admin`, `classroom-roles-public`만 배포했고 두 함수 모두 ACTIVE/version 1이다.
- `tests/integration/classroomRoles.smoke.mjs`의 5개 실제 서버 검사 그룹이 통과했다: 교사 인증/소유자 격리, 암호화/RLS/직접 쓰기 차단, 공용 제출·재제출/위조 거부, 교사 정정·초기화/충돌 처리, 과거 스냅샷/공유 중지/링크 재발급.
- 시험에서 만든 가상 교사 2명과 종속 1인 1역 자료를 모두 삭제하고 잔여 학급 행이 없음을 확인했다. 기존 교사·학생 자료는 읽거나 변경하지 않았다.
- Vercel 미리보기는 로그인 보호 화면이므로 접근 제한을 우회하지 않았다. 운영 프런트엔드의 전체 흐름은 main 통합 후 별도로 확인했다.

### 운영 프런트엔드 인수 결과 (2026-09-27)

- [운영 1인 1역](https://schooldoc-nine.vercel.app/tools/classroom-roles) 배포 및 비로그인 교사 접근 차단 확인. Production 배포 ID `6681691305`, 상태 `success`.
- 실제 Supabase 시험 계정 세션으로 설정 명단 저장 → 2단계 배정/잔여 자리 → 공용 주소 생성 확인.
- 독립 비로그인 학생 컨텍스트에서 이름 선택 → 했어요 저장 → 명단 복귀, 교사 컨텍스트에서 새로고침 → 기록 확인 → 결석 정정 → 월간 상세의 학생 자기보고 표시까지 통과.
- 운영 설정의 공유 중지, 공용 링크 재발급, 이전 학생 링크 차단을 브라우저에서 확인.
- 프런트엔드 페이지 오류 0건, 학생 모바일 뷰포트 가로 넘침 없음. 테스트는 Google OAuth 로그인 화면 자체를 자동화하지 않는다.
- 원격 스모크 7개 검사 그룹 통과. 매 실행에서 생성한 가상 계정 2개와 종속 학급 자료를 모두 정리했다.
- 인수 검사에서 발견한 테스트 선택자 경합을 수정했다. 저장 안내와 로딩 안내가 공존할 때 저장 메시지로 정확히 선택하도록 변경했으며 앱 기능 코드는 변경하지 않았다.
- 수정 후 데모 E2E 22개, 핵심 단위 15개, 린트 통과. 기존 코드 린트 경고 6건은 유지했다. 후속 변경은 테스트·문서뿐이므로 앱/서버 전체 빌드는 앞선 동일 코드 검증 결과를 유지한다.
- 가상 담임 6명의 최종 관점별 인수 결과는 `development-review.md`에 기록했다. 실제 교사 인터뷰나 외부 리뷰로 간주하지 않는다.
